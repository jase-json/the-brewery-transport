import * as React from 'react';
import Button from './Button';

const Menu = () => {
    return (
        <>
            <Button text={1}/>
            <Button text={2}/>
            <Button text={3}/>
        </>
    )
}

export default Menu;